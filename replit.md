# Running this project

This is a static HTML/CSS site. Replit runs it with the `Start application`
workflow:

```bash
python3 server.py
```

The site is served from the project root on port 5000 with browser caching
disabled so the workspace preview always shows the latest files. There are no
package dependencies or environment variables required.